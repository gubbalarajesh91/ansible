- name: variable From Files
  hosts: localhost
  vars_file:
  - vars.yaml
  tasks:
  - name: print override information
    ansisble.builtin.debug:
      msg: "I am learing {{Course}} from Join {{Class}} at {{Time}}"